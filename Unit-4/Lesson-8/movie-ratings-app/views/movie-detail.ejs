<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %> - Movie Ratings & Reviews</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header class="bg-dark text-white py-3 text-center">
        <h1>Movie Ratings & Reviews</h1>
        <nav>
            <ul class="nav justify-content-center">
                <li class="nav-item"><a href="/" class="nav-link text-light">Home</a></li>
                <li class="nav-item"><a href="/submit-review" class="nav-link text-light">Add Review</a></li>
            </ul>
        </nav>
    </header>
    <main class="container mt-4">
        <section>
            <div class="row">
                <div class="col-md-8">
                    <h2><%= movie.title %></h2>
                    <img src="<%= movie.thumbnail %>" class="img-fluid" alt="<%= movie.title %> Thumbnail">
                    <p class="mt-4">Rating: <%= "★".repeat(movie.rating) %><%= "☆".repeat(5 - movie.rating) %></p>
                    <p><%= movie.description %></p>
                </div>
                <div class="col-md-4">
                    <h3>Reviews</h3>
                    <% if (reviews.length > 0) { %>
                        <ul class="list-group">
                            <% reviews.forEach(function(review) { %>
                                <li class="list-group-item">
                                    <strong><%= review.reviewer %></strong>: <%= review.comment %> 
                                    <br>Rating: <%= "★".repeat(review.rating) %><%= "☆".repeat(5 - review.rating) %>
                                </li>
                            <% }); %>
                        </ul>
                    <% } else { %>
                        <p>No reviews yet. Be the first to add one!</p>
                    <% } %>
                </div>
            </div>
        </section>
    </main>
    <footer class="bg-dark text-white text-center p-3 mt-4">
        © 2024 Movie Ratings & Reviews
    </footer>
</body>
</html>
